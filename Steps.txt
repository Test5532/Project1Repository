GIT

1 create Folder in ur machine for loacl repo
2 init it
3 add a origin/source to it to pull and push code from and to
4 now create a new file in folder you cretaed  git status >git add -a/"filename" > git commit -a -m "message"
this will commit to local repository for tracking
5 to push code to origin >  git push origin master
5 to pull code from origin  > git pull origin master